<?php

return [
    // مسیر ذخیره‌سازی آپلودها. اگر با «/» شروع شود، مسیر مطلق در نظر
    // گرفته می‌شود؛ در غیر این صورت نسبت به ریشه‌ی پروژه محاسبه می‌شود.
    // پیش‌فرض (اگر این کلید حذف شود): «public/uploads» —‌ یعنی هر فایل
    // بلافاصله از طریق یک URL مستقیم قابل‌دسترس است.
    //
    // برای این‌که فایل‌ها را از دید مستقیم عموم خارج کنی و به‌جایش از
    // طریق یک Route اختصاصی (با کنترل دسترسی دلخواه خودت) سرو کنی،
    // کافی‌ست این مسیر را خارج از public قرار بدهی، مثلاً:
    //   'upload_path' => __DIR__ . '/../storage/uploads',
    'upload_path' => __DIR__ . '/../public/uploads',

    // فرمت‌های مجاز، بر اساس نوع محتوای واقعی فایل (نه پسوند نام فایل).
    // این‌جا لیست پیش‌فرض فریم‌ورک تکرار شده؛ هر فرمتی که نمی‌خواهی
    // پذیرفته شود را حذف کن، یا فرمت جدید (مثلاً 'image/svg+xml') اضافه کن.
    'allowed_mime_types' => [
        'image/jpeg',
        'image/png',
        'image/webp',
        'image/gif',
    ],

    // حداکثر حجم مجاز هر فایل، بر حسب بایت
    'max_size_bytes' => 5 * 1024 * 1024, // 5MB
];
